#include "lists.h"
/**
 * insert_node - inserts a node in a list
 * @head: start of list
 * @number: number to be filed
 * Return: address of new node
 */
listint_t *insert_node(listint_t **head, int number)
{
	while
}
